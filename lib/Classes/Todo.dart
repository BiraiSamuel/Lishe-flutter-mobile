class Todo {
  final int id;
  final String title;
  final String description;
  final String type;
  final String price;
  final List images;
  final List variants;
  final List options;

  Todo(this.id, this.title, this.description, this.type, this.price, this.images, this.variants, this.options);
}